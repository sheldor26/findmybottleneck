"""A desktop window for findmybottleneck.

Optional: needs `pip install findmybottleneck[gui]`. Nothing in the rest of
the package imports this — the CLI's `capture`/`explain`/`compare`/`overlay`/
`check` stay standard-library-only whether or not this is installed.
"""
